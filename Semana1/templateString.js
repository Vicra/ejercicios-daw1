let nombre = 'victor'
let segnombre = 'ricardo'
let apellido = 'ramirez'
let apellido2 = 'perdomo'

let nombreCompleto = nombre + ' ' + segnombre + ' ' + apellido + ' ' + apellido2 + ' ' + segnombre + '\n' + apellido + ' ' + apellido2;
// console.log(nombreCompleto)

let nombreMasiso = 
    `${nombre} ${segnombre} ${apellido} ${apellido2}`
console.log(nombreMasiso)

/*
let insertSQL = `INSERT INTO table
(
    name
    , phone
    , address
    , image
    , url
)
VALUES
(
    ${name}
    , ${phone}
    , ${address}
    , ${image}
    , ${url}
)`;
*/